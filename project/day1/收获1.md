

html 解析方法

```python

firebug :
    用于 Web 前端开发的工具，它是 FireFox 浏览器的一个扩展插件。它
可以用于调试 JavaScript、查看 DOM、分析 CSS、监控网络流量以及进行 Ajax 交互等。它提供了几乎前端开发需要的全部功能

爬虫领域：
	分析协议和分析动态网站
	

```



控制面板：

```python
console.log   记录日志
console.debug  记录调试信息+ 超链接
consol.error    消息前线是错误图标 + 超链接
console.info    消息前小时消息图标 + 超链接
console.warn    再纤细线显示警告 + 超链接

document.write(a,b);   //网页上输出内容


--》 consloe 控制面板提示信息  （监听网络访问 + 响应）

HTML、CSS、JavaScript、XHR、图片等选项，将所有的网络访问进行了分类划分
```

调试：

```python
JavaScript 内容上方的 四个按钮已:
    	继续执行<F8>：当通过断点来停止执行脚本时，单击<F8>就会恢复执行脚本
 		单步进入<F11>：允许跳到页面中的其他函数内部
 		单步跳过<F10>：单击<F10>来直接跳过函数的调用即跳到 return 之后
 		单步退出<shift+F11>：允许恢复脚本的执行，直到下一个断点为止	
```

xapth 选择器插件：

```python
chrome:  xpath
    
fireFox   firepath
```

